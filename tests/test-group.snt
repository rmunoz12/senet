@setup {

group A() {
    int x;

    func void __init__() {
        this.x = 2;
    }
};

group A obj;

}

@turns {

func void begin() {
    # group A obj;
    obj.__init__();
    print(obj.x); print("\n");
    obj.x = obj.x + 1;
    print(obj.x); print("\n");
    end;
}

}
